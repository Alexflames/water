Рекурсивные алгоритмы. 

Базис:
Z(x1, ...., xn) = 0
S(x) = x + 1        -- следования
Inm = (x1, x2,..., xm, xm+1 ..., xn) = xm

Оператор суперпозиции - позволяет перейти от функции с n перменными к функции с m переменными. 
Сохраняет вычислимость и всюду определенности. 

Пример:
f(x) = 0  g(x) = x + 1
Получим h(x) = g(f(x)) = 0 + 1 = 1

Оператор примитивной рекурсии
f(x1, ..., xn, 0) = g(x1, ..., xn)
f(x1, ..., xn, y + 1) = h(x1, ..., xn, (не дописал))

f(x1, ..., xn, y) = R(g(x1, ..., xn), h(x1, ...., xn, y, z))
f = R(g, h)

Функция называется примитивно рекурсивной, если:
1) Элементарная
2) Функция может быть получена из элементарной с помощью конечного числа применения операции суперпозиции и примитивной рекурсии

Как доказать примитивную рекурсивность?
Зачем? Потому что это значит, что она вычислима

а) простейшая б)построена с помощью суперпозиции в) с помощью примитивной рекурсии

Пример:
1) Функция - константа
f(x) = m s(s(s...s(Z(x)))) m-раз

2) Сложение
...
3) Умножение
f(x, y) = x * y
f(x, 0) = x * 0 = 0
f(x, y + 1) = x * (y + 1) = x * y + x = f(x, y) + x
Док-во:
f(x, 0) = g(x) = 0 = Z(x);
f(x, y + 1) = h(x, y, z) = h(x, y, f(x, y)) = x + z = I-3-1 (x, y, f(x, y)) +
 I-3-3(x, y, f(x, y))
 x^2 = R(Z, [+; I-3-3, I-1-3])

4) (не записал)

Оператор минимизации g^(n+1) (x1, x2, ..., xn, y)
Определяет, при каком значении набора (x1, ..., xn) 'y' равен 0

Такое не всегда можно найти. Но 100% можно, если функция определена для всех z <= y

Используя оператор минимизации можно получать частично-определенные функции.

Тезис Черча.

Функция называется частично-рекурсивной (вычислимой по Черчу), если она может быть получена из простейших функций с помощью конечного числа операторов суперпозиции, примитивной рекурсии и минимизации

Введем обозначения:
Кпрф - класс примитивно рекурсивных функций
....(не записал)
....

Тезис Черча 
Класс алгоритмически вычислимых функций совпадает с классом всех частично рекурсивных функций... (не записал)

Всякая функция ...
.... (не записал)

================================
Определения:
Алфавит - любое непустое множество символов. Его элементы называются буквами, а любые последовательности букв - словами. Пустое слово обозначается /\

Если А и B - 2 алфавита причем А <= B, то алфавит B называется расширением А

Марковские подстановки - это...

Заключительная подстановка - с точкой на конце

138 578 926 
vvvvvvvvvvvvvv
Подстановка (8 578 9, 00)
vvvvvvvvvvvvvv
Результат - 130 026

Нормальные алгоритмы Маркова

a -> ./\
b -> b

aab = ab, baba = bba, ab = b, bb = bb

Также можно делимость на 3 сделать, круто, да

Принцип нормализации Маркова.
Для нахождения значения функции..(не записал)

Инфа про доклады  
