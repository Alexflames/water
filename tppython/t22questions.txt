Дан фрагмент программы, проверяющий, состоит ли строка только из чисел или букв. 
Что должно быть на месте пропуска?

x = input()
if x.isalpha():
	print("Строка состоит только из букв)
__ x.isdigit():
	print("Строка состоит только из цифр)
~
+ elif

Выберите правильный вариант импортирования модуля на языке Python
#
+ import math
- include math
+ from math import *
- using math

Дана строка abcde, укажите фрагмент кода который должен 
быть на месте пропуска чтобы программа вывела abcd?

a = abcde
print(_)

%
- a - 1
- a - 'e'
+ a[:-1]
- a[::-1]

Выберите правильные варианты
В Python элементы в множестве(set):
#
- расположены в порядке возрастания
- расположены в порядке убывания
- расположены в порядке добавления в множество
+ не упорядочены
- повторяются
+ уникальные

Для сортировки итерируемых объектов в Python используется
#
+ функция sorted()
+ метод list.sort()

Выполнение какой конструкции после циклов for/while 
происходит если внутри цикла не сработал break?
~
+ else

Узнать длину строки str в Питоне можно с помощью:
%
+ len(str)
- str.len()
- str.length()
- str.size()
