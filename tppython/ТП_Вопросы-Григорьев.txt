1. Дан фрагмент программы, проверяющий, состоит ли строка только из чисел или букв, что должно быть на месте пропуска?

x = input()
if x.isalpha():
	print("Строка состоит только из букв)
__ x.isdigit():
	print("Строка состоит только из цифр)

Ответ: elif

2. Выберите правильный вариант импортирования модуля на языке Python
+ import math
- include math
+ from math import *
- using math

3. Дана строка abcde, укажите фрагмент кода который должен быть на месте пропуска чтобы программа вывела abcd?

a = abcde
print(_)

- a - 1
- a - 'e'
+ a[:-1]
- a[::-1]

4. Выберите правильные варианты
В Python элементы в множестве(set):
- расположены в порядке возрастания
- расположены в порядке убывания
- расположены в порядке добавления в множество
+ не упорядочены
- повторяются
+ уникальные

5. Для сортировки итерируемых объектов в Python используется
Возможные ответы: 
+ функция sorted()
+ метод list.sort()

6. При каком условии выполнится поставленная после цикла for или while инструкция else?
Если внутри цикла не использовался break

7. Узнать длину строки str в Питоне можно с помощью:
+ len(str)
- str.len()
- str.length()
- str.size()
